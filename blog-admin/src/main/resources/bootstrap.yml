spring:
  profiles:
    active: @profile.active@
  application:
    name: blog-admin
  cloud:
    nacos:
      discovery:
        #注册中心地址
        server-addr: http://192.168.156.24:8848
      config:
        # 配置中心地址
        server-addr: http://192.168.156.24:8848
        # 修改文件扩展名
        file-extension: yaml
        # 缺省即可
        group: DEFAULT_GROUP
        # 缺省即可: 配置文件名前缀
        prefix: ${spring.application.name}
